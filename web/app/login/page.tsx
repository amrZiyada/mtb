'use client'
import {useState} from 'react'
import {api} from '../../lib/api'
export default function Login(){const [username,setUsername]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function go(e:any){e.preventDefault();try{const result=await api<{token:string}>('/login',{method:'POST',body:JSON.stringify({username,password})});localStorage.setItem('mtb_user_token',result.token);location.href='/dashboard'}catch(e:any){setMsg(e.message)}}return <main className="min-h-screen grid place-items-center bg-slate-50 p-4"><form onSubmit={go} className="w-full max-w-sm rounded-2xl border bg-white p-6 shadow"><h1 className="text-2xl font-bold">Staff login</h1><p className="mt-1 text-sm text-slate-500">Doctor · Medical Representative · Salesman</p><input required value={username} onChange={e=>setUsername(e.target.value)} placeholder="Username" className="mt-5 w-full rounded-xl border px-3 py-3"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="mt-3 w-full rounded-xl border px-3 py-3"/><button className="mt-4 w-full rounded-xl bg-slate-900 py-3 font-semibold text-white">Login</button>{msg&&<p className="mt-3 text-sm text-red-700">{msg}</p>}</form></main>}
